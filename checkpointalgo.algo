ALGORITHM chekpointe algo
VAR
 var set1=[3, 1, 7, 9]
 var set2= [2, 4, 1, 9, 3]
 var arr= set1 + set2
BEGIN
  let distinctElements = [];
    for (let i = 0; i < set1.length; i++) 
    if (distinctElements.indexOf(set1[i]) === -1)
    write ()
     for (let i = 0; i < set2.length; i++)
    if (distinctElements.indexOf(set2[i]) === -1) 
      distinctElements.push(set2[i]);
  let sum = 0;
  for (let i = 0; i < distinctElements.length; i++) 
    sum += distinctElements[i];
END